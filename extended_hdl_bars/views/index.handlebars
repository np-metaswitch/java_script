{{> lovedChamps}}
{{> hatedChamps}}
<h2>Similar players : {{proPlayer}}</h2>
{{#if listExists}} 
  <ul>
    {{#each suggestedChamps}}
      <li class={{this.lane}}>{{this.name}}</li>
    {{/each}}
  </ul>
{{/if}}